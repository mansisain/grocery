import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  continueButton: {
    flexDirection: 'row',
    justifyContent: 'space-around',
    height: 80,
    paddingTop: 35,
  },
  btnImage: {
    width: '30%',
    height: 45,
  },
});
